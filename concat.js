// The concat() method creates a new array by merging (concatenating) existing arrays
const myGirls = ["sri", "latha"];
const myBoys = ["krishna", "Tinku"];

const students = myGirls.concat(myBoys);
console.log(students);