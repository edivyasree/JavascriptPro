// Move the last node to the front of a linked list
// Google Translate Icon
// Given a linked list, move its last node to the front.
// For example, list {1, 2, 3, 4} should be changed to {4, 1, 2, 3}.

let arr=[1, 2, 3, 4]
let arr1=arr.pop()
console.log(arr);
let ar4=arr.unshift(4)
console.log(arr);
